<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elfalt.ledo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar">
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".LoginScreenActivity" />
        <activity android:name=".ui.JourneyLessonActivity" />
        <activity
            android:name=".ui.JourneyActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".ui.HomeScreenActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.QuestionsActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".ui.CongratulationsActivity"
            android:theme="@style/NoActionBar" />
    </application>

</manifest>